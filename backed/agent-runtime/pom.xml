<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.strato</groupId>
    <artifactId>strato-backed</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>agent-runtime</artifactId>
  <name>agent-runtime</name>
  <!-- 决策面：依赖 spi / contracts；对 registry、gateway 只用其 application 包的用例类（进程内适配，见 infra/inprocess；抽接口 + HTTP 适配为后续 change）；不依赖 domains/* -->
  <dependencies>
    <dependency><groupId>com.strato</groupId><artifactId>platform-spi</artifactId></dependency>
    <dependency><groupId>com.strato</groupId><artifactId>contracts-java</artifactId></dependency>
    <dependency><groupId>com.strato</groupId><artifactId>tool-registry</artifactId></dependency>
    <dependency><groupId>com.strato</groupId><artifactId>tool-gateway</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-web</artifactId></dependency>
    <dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-validation</artifactId></dependency>
    <!-- LLM 只经 Spring AI（backend-standard §1）；禁止 Spring AI Alibaba / LangChain4j。
         用库而非 starter：starter 的自动配置会在无 API key 时拒绝启动（speech / image / embedding 全部要 key），
         与"无 key 回退规则规划器"冲突；ChatClient 由 LlmConfiguration 手工装配。 -->
    <dependency><groupId>org.springframework.ai</groupId><artifactId>spring-ai-openai</artifactId></dependency>
    <dependency><groupId>org.springframework.ai</groupId><artifactId>spring-ai-client-chat</artifactId></dependency>
  </dependencies>
</project>
