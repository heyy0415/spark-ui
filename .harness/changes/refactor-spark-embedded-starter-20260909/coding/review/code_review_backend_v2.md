# Code Review (Backend) v2 — refactor-spark-embedded-starter-20260909

- 评审对象：回修 commit `dc0ab7f`（`git diff dc0ab7f^ dc0ab7f -- spark-rooter .harness/scripts`），对照 `code_review_backend_v1.md`（4 MUST / 10 SHOULD）与 `coding_report_v1.md` 末段「阶段 4 回修记录 v1」
- 模式：execution / 第二轮逐条核对
- 方法：逐文件阅读回修 diff 与当前代码；`node .harness/scripts/check-module-deps.mjs` 实跑（exit 0）；对门禁做三项实验（① runtime 植入 `String userId` 临时文件 → 红后删除；② 把脚本里真实 peer 规则正则临时改回 `com\.spark\.` 跑副本 → **仍 exit 0**；③ 把真实 examples 跨领域正则临时改回 `com\.sparkrooter\.domain\.` 跑副本 → **仍 exit 0**；副本与植入文件已删除，`git status` 干净）；对 `IntentVerbs.target` / `RunOrchestrator.effectiveMessage` 做静态推演。未修改代码，未启动 JVM。
- 评审人：独立后端评审（Claude）· 日期 2026-09-10

## 1. v1 意见逐条核对

| # | v1 意见 | 结论 | 证据 |
|---|---|---|---|
| M-1 | `check-module-deps` 身份红线未实现；三条规则正则旧包名静默失效；要求每条规则内置「植入即红」自测 | **部分** | ① `IDENTITY = /\b(userId\|tenantId\|Principal)\b/` 覆盖 7 个平台模块（`PLATFORM_SRC`，含 web-mvc / starter）；实验①植入 runtime `String userId` → `✗ … must not reference userId / tenantId / Principal`，exit 1 ✅。② 三处正则已改 `com\.sparkrooter\.` / `examples\.`（`:71` `:159` `:197`）✅，node 实测 `import com.sparkrooter.gateway.infra.LogAuditSink;` 命中、`gateway.api` 不命中 ✅。③ **自测未达目的**：`selfTests`（`:118-125`）里的 peer 规则、examples 规则、pom 规则是**手抄的副本字面量**，不是 `:159` / `:197` / `:71` 处真正执行的正则；实验②③把真实规则改回旧包名，自测全绿、脚本 exit 0。只有 `STEREOTYPES` / `IDENTITY` 两条因为是共享常量才真正受自测保护。v1 要抓的正是「改名后正则静默失效」，这两条规则再失效一次仍不会被发现 → 见 N-1 |
| M-2 | spi `OrderSnapshotProvider.snapshot(String tenantId, …)` 含 `tenantId`；报告不实 | **已解决** | `OrderSnapshot(Provider)` 已迁到 `spark-rooter/examples/demo-support/src/main/java/com/sparkrooter/examples/support/`；spi 目录两文件删除；4 个消费方 import 改 `examples.support`；平台 7 模块 `grep -w userId\|tenantId\|Principal` = 0（本轮实跑），且由 IDENTITY 门禁守住；`project-structure.md:84` / README 同步。门禁 `:198-199` 把 `examples.support` 列为共享支撑模块放行 ✅（提示文案残留见 N-5） |
| M-3 | 「user text in log」断言短语无人发送，恒 0 | **已解决** | `e2e-backend.sh:362-364` 对 5 条短语循环 `grep -c == 0`；核对 5 条都确实在本轮发送：`帮我把订单 10001 退款` 经 `--data @intent-request.example.json`（`:83`，示例文件正文即该句）、`删除订单 10010`（⑭）、`最近 5 单已发货的订单`（⑱）、`查看订单 10002 的物流`（⑧ ㉑ ㉓）、`有什么商品`（⑨）；`deploy-verify.sh:51` 改为示例文件实际发送的句子 ✅ |
| M-4 | 澄清屏后 `remember()` 用 `LastTable("", ids)` 覆盖，「第二个」失效 | **已解决**（引入新回归 N-2） | 推演「申请售后」→「第二个」：`clarify()` 写 `Memory(aftersale, {}, LastTable(order.list.search, ids, "申请售后"))` 并 `clarified.add`（`RunOrchestrator:651-656`）→ `complete → remember` 命中 `clarified.remove` 直接 return（`:667-669`），不再覆盖 ✅。下一轮「第二个」：规则路由 none → `isOrdinalReference` 为真 → 沿用记忆 `aftersale`，`source=memory`（`:153-164`）→ `fillOrdinal` 经 `meta.find("order.list.search").clarifiesEntity=ORDER` 解析 rows[1] ✅ → `effectiveMessage` = `"申请售后 第二个"` → `IntentVerbs.target` 命中 `aftersale.create` → 确认屏。e2e ㉒'（`:321-327`）3 条断言均为真实断言（标题含 rows[1].id、日志 `source=memory(ordinal)`）。**并发 / 泄漏**：`clarified` 按 runId，clarify 与 remember 同线程同 Run；clarify 成功后必走 `complete`，remove 成对；若中途 `fail()` 未 remember，条目随 `evictExpired()` 清理（`:834`），有界 ✅。**pendingMessage 何时清除**：下一次对同 `(sessionId, conversationId)` 的 `memory.put` 覆盖（普通列表 `LastTable(…, null)`）；若后续 Run 不写记忆（无能力提示 / 失败）则一直挂到 memoryTtl（见 N-3）。**普通列表后「第二个」**：`remember` 写 `domain=plan.domain`，「第二个」无领域词 → 记忆领域 → `fallbackTarget(order, hasEntity=true)` → `order.detail.get`，仍工作 ✅；「第二个的物流」（e2e ⑳）走规则路由不受影响 ✅ |
| S-1 | Runtime 发现路径给 `ToolAccessPolicy` 的 sessionId 恒 null | **已解决** | `ToolSearchPort.search(Request, String sessionId)` / `ToolRegistryClient.search(Request, String)`；`RunOrchestrator:173-174` 透传 `sessionId`；`SearchToolsUseCase.execute(req, sessionId)` → `access.allowed(toolId, sessionId)`；HTTP `/internal` 直调与自检传 null 并在 Javadoc 注明 |
| S-2 | 记忆只按 conversationId 键控 | **已解决** | spi `ConversationMemory.put/find(sessionId, conversationId, …)`；`InMemoryConversationMemory.key = sessionId + "/" + conversationId`；编排器 3 处调用全带 `run.sessionId()` / `sessionId`；e2e ㉔ 末条：B（`X-Demo-User: userb`）用 A 的 `c21` 说「申请售后」→ `['Table']` 澄清屏而非 A 的 10002 确认屏，是真实隔离断言（e2e 主进程以 `--spring.profiles.active=e2e` 启动，`DemoSessionIdResolver` 生效，`:33`） |
| S-3 | 双绑定退化单因子；㉔ 只测 runId 绑定 | **已解决**（文档残留 N-6） | `host-demo/DemoSessionIdResolver`（`@Profile("e2e")`，`userId + ":" + conversationId`）；默认 profile 仍走 starter demo 实现 + WARN（`RuntimeBeans:57`），deploy-verify 用默认 profile 不受影响。㉔ 现为真实 HTTP 级断言：A 的令牌 + `X-Demo-User: userb` 提交 → `AgentRunController.confirm` 解析出 `userb:c24` ≠ `run.sessionId()` → `RunOrchestrator:290` `rejectRequest("session mismatch")` → `CONFIRMATION_REJECTED`；`:334` grep `runId=… .*session mismatch` 命中 `rejectRequest` 的 WARN 行；`:335` B 读 A 的 Run 404；`:336` 无 `refund.create` 审计。会话不一致确实由请求侧身份触发 ✅。但 `ConfirmationTokenService.consume` 的 conversationId 一侧仍取自 Run 自身（`AgentRunController:91`），恒等；`:91` 注释「会话号可被前端伪造」与实际不符 → N-6 |
| S-4 | 澄清 intent 回拼原话：超长截 id / 含 `<` → INTERNAL_ERROR | **已解决** | `ClarificationScreen:81` `intent = truncate(verbLabel + " " + entityLabel + " " + id, 200)`；`verbLabel ≤ 32`，id 恒在；原话不再进 intent，`<` / `://` 不可能出现。无动词时 label 为「选择」→ intent「选择 订单 10002」下一轮按「订单」路由到 order 详情（领域可能漂移，但无失败路径，LOW 不单列） |
| S-5 | 自检 EchoTools 以 active 进 Registry，污染候选与 `domains()` | **已解决** | `ManifestDeriver:94-95` 领域 == `spark` → `status=draft`；`DiscoveryPolicy:15` 只放 active / canary → `search` 与 `domains()` 均排除；Gateway 走 `ToolResolver.resolve → repo.find(toolId, version)`（`RegistryToolResolver:22`），无状态过滤，`InvokeToolUseCase` 亦未按 status 拒绝 → `ProxyInvocationSelfCheck` 直调仍通 ✅。`spark` 域按字符串相等保留（N-7） |
| S-6 | `RunRepository` 无 TTL；`lastUi` 只增不减 | **已解决** | `InMemoryRunRepository(ttl, clock).evictExpired()` 按 `updatedAt + ttl`；`spark.runtime.run-ttl` 默认 1h；`RunOrchestrator.evictExpired()` 在 `complete` / `fail` 清 `lastUi / stepOutputs / inputSchemas / confirmLocks / clarified`。**WAITING_CONFIRMATION 误删分析**：`updatedAt` 在转入 WAITING_CONFIRMATION 时刷新（`Run.transition`），此后无更新；默认 tokenTtl 10m < runTtl 1h，被淘汰时令牌必已过期，不会误删有效等待中的 Run ✅。仅当宿主把 `run-ttl` 配得小于 `token-ttl` 时，持有效令牌的确认会变成 404 而非 CONFIRMATION_REJECTED（N-4）。淘汰只由终态事件触发、无定时器：空闲进程不回收，可接受 |
| S-7 | 校验器另起 `JsonSchemaFactory` + 裸 mapper | **已解决** | `ToolSelectionValidator.assertValueMatches(…, SchemaValidator)` 用 `validator.validateWithInlineSchema` 与 `validator.mapper()`；本地工厂 / MAPPER 删除；`RuleBasedLlmClient` / `SpringAiLlmClient` / `PlanSelfCheck` 注入。`validateWithInlineSchema` 内部仍每次 `factory.getSchema(inline)` 编译（`SchemaValidator:140-144`），v1 顺带提到的重复编译未变（N-9，INFO） |
| S-8 | 组件注解规则可被 FQN 绕过 | **已解决** | `STEREOTYPES = /@(?:org\.springframework\.(?:stereotype\|context\.annotation)\.)?(Component\|…)\b/`，node 实测 FQN 内联为真；自测含 FQN 样本且使用同一常量 ✅。代价：去掉了原 `^\s*` 行首锚点，注释里的「@Configuration」也会命中（本轮已把 `SparkTool` Javadoc 改成 `{@code Configuration}` 才过）→ N-12 |
| S-9 | `entityArgs()` 同名不同类型 last-wins 且每次重算 | **已解决** | `ToolMetaRegistry.register` 增量维护 `entityArgs`，`putIfAbsent` 后类型不同 → `IllegalStateException` 启动失败；`entityArgs()` 只做默认表 + 增量表一次合并。默认表 (`orderId→order`) 与注解声明冲突时注解静默覆盖，未检测（LOW，不单列） |
| S-10 | ⑭' 拦截器与 spark 路径无交集，同义反复 | **已解决** | `DemoController./demo/orders` 直接调同一个 `OrderTools.list`（`@SparkTool order.list.search` 的方法）；⑭' 三条：guest 直调 403（拦截器 `/demo/**`）、admin 200、guest 经 spark 同能力 `run.completed`。对照成立 |

## 2. 新发现

| # | 位置 | 问题（含复现） | 建议 | 分级 |
|---|---|---|---|---|
| N-1 | `.harness/scripts/check-module-deps.mjs:118-128` vs `:71` `:159` `:197` | 自测样本表里 pom 规则、peer 规则、examples 规则的正则是**手抄副本**，不是真正执行的那三个字面量。实验：把 `:159` 改回 `` `\\bcom\\.spark\\.${o}\\.` `` 或把 `:197` 改回 `com\.sparkrooter\.domain\.`，自测照样全绿、脚本 exit 0。v1 M-1 的 Hashimoto 诉求「改名后正则失效必须红」对这三条没有落地；下次包名调整会再次静默放行 | 把三条正则提为模块级常量（`PEER_RULE(o)` / `EXAMPLES_RULE` / `POM_DEP_RULE`），规则与自测都引用同一常量；并给每条加一个「必须不命中」的负样本（如 `gateway.api`）防过宽 | SHOULD |
| N-2 | `RunOrchestrator.effectiveMessage:571-579`；`IntentVerbs.target:66-84` | `pendingMessage` 只要当前消息含序数就无条件前拼，**不管当前消息自带动词**。复现（全部为已有 e2e 场景的组合）：`c16`「删除订单」→ 澄清屏（pending=「删除订单」）→ 用户说「第二个的物流」→ 规则路由 order → planMessage =「删除订单 第二个的物流」→ `target()` 第一轮按 order 域顺序匹配，`删除` 先于 `物流` 命中 → 规划为 `order.detail.get → order.delete`，弹出**删除**该订单的确认屏，而用户要的是物流。同理 pending「申请售后」+「第二个删掉」→ 记忆领域 aftersale → 售后确认屏。有确认门兜底不会误执行，但属于规划错向的高风险确认屏，且是本轮回修新引入的路径 | 只在「当前消息本身无动词」时才前拼：`IntentVerbs.target(message, domain).isEmpty()`（或仅当 `source == "memory"`）才使用 pendingMessage；e2e 补「澄清屏 → 『第二个的物流』→ tool == order.logistics.get」 | SHOULD |
| N-3 | `RunOrchestrator.remember:696-698`；`clarify:651-655` | 澄清屏后若下一轮 Run 不写记忆（无能力提示、失败、纯文本），`pendingMessage` 不会被清除，直到 memoryTtl；之后任意含序数的消息都会被拼上旧动词。与 N-2 叠加放大 | 澄清屏的 pendingMessage 被消费（`effectiveMessage` 命中）后立即 `memory.put` 一份 `pendingMessage=null` 的副本；或 remember 在「无可写内容」时也清 pending | LOW |
| N-4 | `SparkRooterProperties.Runtime`（`runTtl` 1h / `tokenTtl` 10m）；`InMemoryRunRepository.evictExpired` | 无 `runTtl >= tokenTtl` 约束：宿主配 `run-ttl: 5m` 时，持有效令牌的 WAITING_CONFIRMATION Run 可能先被淘汰，确认变 404 而非 CONFIRMATION_REJECTED，令牌也不会被消费 | 启动期断言 `runTtl >= tokenTtl`（`IllegalStateException`），或 evict 跳过 `WAITING_CONFIRMATION && updatedAt + tokenTtl > now` | LOW |
| N-5 | `check-module-deps.mjs:200` | 违规提示仍写 `(cross-domain only via spark-rooter-spi)`，与 `:180` 注释及 M-2 迁移后的事实（demo-support）矛盾 | 改为 `only via examples.support OrderSnapshotProvider` | LOW |
| N-6 | `ConfirmationTokenService:91`；`AgentRunController:91,105` | 注释「双绑定：会话号可被前端伪造，sessionId 来自宿主」——实际 `consume` 的 conversationId 取自 Run 自身，与签发值恒等，不构成第二因子；真正拦住跨用户的是 `RunOrchestrator:290` 的 `run.sessionId().equals(sessionId)` 预检（e2e ㉔ 命中的也是这一处）。v1 S-3 给的两个选项（请求侧解析 / 文档如实写单因子）都未采纳 | 注释改为「sessionId 单因子；conversationId 比对为一致性校验」，或 `consume` 去掉 conversationId 参数 | LOW |
| N-7 | `ManifestDeriver:43,95` | `SELFCHECK_DOMAIN = "spark"` 用字符串相等判定 draft：宿主若把业务工具声明为 `domain="spark"`，会被静默注册为 draft、不可发现、无任何日志 | 扫描期对非 starter 包声明的 `domain=spark` 直接启动失败（保留域），并在 `SparkTool` Javadoc 注明 | LOW |
| N-8 | `RunOrchestrator:569-570` | 两段连续 `/** … */`：「规划；目标缺实体时…」原属 `planWithMemory`，现悬挂在 `effectiveMessage` 上方，Javadoc 归属错乱 | 把第一段移回 `planWithMemory` 上方 | LOW |
| N-9 | `SchemaValidator.validateWithInlineSchema:140-144` | 每次调用 `factory.getSchema(inlineSchema)` 重新编译；`assertValueMatches` 每参数每次规划都走这里。S-7 只是把重复编译从 runtime 挪进 contracts | 以 `inlineSchema` 的 hash / identity 做 `ConcurrentHashMap` 缓存 | INFO |
| N-10 | `RunOrchestrator:148-164`；`DomainResolver.resolve` | 纯序数消息先跑 `resolver.resolve()`（LIVE 模式会先打一次模型分类 + `registry.domains()`），失败后才查记忆；分类器对「第二个」大概率 none，白付一次模型延迟 | 先判 `isOrdinalReference && remembered.domain` 再走 resolver | LOW |
| N-11 | `e2e-backend.sh:38` | 「14 tools registered」把 draft 的 `spark.selfcheck.echo` 计在内，注释仍写「+ selfcheck echo」；与 S-5 后「不可发现」的语义不冲突，但建议补一条 `/internal/tool-registry/search {domain:"spark"}` → `tools == []` 的显式断言，让 S-5 有验收 | 补断言 | INFO |
| N-12 | `check-module-deps.mjs:90-91` | `STEREOTYPES` 去掉了原 `^\s*` 行首锚点（`m` 标志），注释 / Javadoc 中的「@Configuration」「@Component」都会命中；本轮 `SparkTool.java` Javadoc 被迫改写为 `{@code Configuration}` 即证据。后续平台源码注释里提到这些注解都会误红 | 恢复 `^\s*@(...)` + `m`，FQN 前缀保留在锚点之后 | LOW |

## 3. Verdict

**APPROVED（附 2 条 SHOULD 后续项）** — 0 MUST FIX / 2 SHOULD / 8 LOW / 2 INFO。

v1 的 4 条 MUST：M-2 / M-3 / M-4 已解决；M-1 的门禁本体（IDENTITY 规则、三处正则、FQN）已正确且植入即红，但「自测防正则漂移」对 3 条规则没有真正生效（N-1，降为 SHOULD，因为当前正则本身是正确的）。10 条 SHOULD 全部解决，S-3 / S-7 遗留文档与性能小尾巴（N-6 / N-9）。M-4 的修复路线（`clarified` 集合 + `pendingMessage` + 记忆领域路由）对报告的场景成立且 e2e ㉒' 为真实断言，但 `pendingMessage` 无条件前拼带来一处新的规划错向（N-2），建议与 N-1 一并在阶段 5 前处理。

## 4. 建议补丁清单（按优先级）

1. `check-module-deps.mjs`：三条正则提为常量并被规则与自测共同引用；加负样本。（N-1）
2. `RunOrchestrator.effectiveMessage`：仅当当前消息无自身动词时前拼 `pendingMessage`；e2e 补「澄清屏 → 第二个的物流 → logistics」。（N-2）
3. 消费后清 `pendingMessage`；`runTtl >= tokenTtl` 启动断言。（N-3 / N-4）
4. 文案与注释：`:200` 提示、`ConfirmationTokenService:91`、悬挂 Javadoc、`STEREOTYPES` 行首锚点。（N-5 / N-6 / N-8 / N-12）
