# OS / IDE
.DS_Store
.idea/
*.iml
.vscode/

# 本地会话与个人配置（保留 .claude/settings.json 若存在）
.claude/settings.local.json

# 子工程各自的忽略见 spark-ui/.gitignore、spark-rooter/.gitignore、.harness/.gitignore

# CI 占位 change 的产物（SPARK_CHANGE=ci-run，见 .harness/changes/ci-run/summary.md）
.harness/changes/ci-run/deployment/
