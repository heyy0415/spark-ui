# 安全问题报告

发现安全问题请**不要**开公开 issue。

发邮件到仓库 owner（GitHub 主页有联系方式），或用 GitHub 的「Report a vulnerability」（Security → Advisories）。写清楚：影响的模块、复现步骤、你认为的影响范围。一般会在 72 小时内回复。

## 支持范围

只维护 `main` 分支。本项目不发包，使用者 clone 后自行构建，所以没有"哪些版本受影响"的概念——拉最新的 `main` 即可。

## 这个项目里什么算安全问题

- 模型输出绕过校验直接执行了工具（`PlanValidator` / Gateway 输入校验被绕过）
- 高风险工具在没有有效确认令牌的情况下执行；令牌重放成功；跨会话的令牌被接受
- 前端能把非自然语言的内容送进后端并被当成指令
- provider 注册 / 调用绕过了 `ProviderAuth`
- 日志或审计里出现了密钥、用户原文、模型网关地址
- 多副本部署下幂等失效导致写操作重复执行

不属于安全问题、走普通 issue 的：模型理解得不对、UI 渲染问题、性能。

## 设计边界

内核不认识用户，身份、权限、租户全在宿主。宿主自己的鉴权漏洞不在本项目范围，但如果你发现内核让宿主**很容易**犯这类错（比如某个默认值不安全），也请报——那是我们的问题。安全模型全文在 [`agent-safety.md`](.harness/rules/agent-safety.md)。
